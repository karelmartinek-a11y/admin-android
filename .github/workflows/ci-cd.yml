name: CI/CD

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: gradle
      - uses: android-actions/setup-android@v3
        with:
          cmdline-tools-version: 12266719
          accept-android-sdk-licenses: true
          log-accepted-android-sdk-licenses: true
          packages: "platforms;android-34 build-tools;34.0.0 platform-tools"
      - name: Prepare keystore
        run: |
          echo "${{ secrets.KEYSTORE_B64 }}" | base64 -d > /tmp/keystore.jks
          test -s /tmp/keystore.jks
      - name: Build (test + release)
        env:
          HOTEL_STORE_FILE: /tmp/keystore.jks
          HOTEL_STORE_PASSWORD: ${{ secrets.STORE_PASSWORD }}
          HOTEL_KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          HOTEL_KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
        run: |
          ./gradlew :adminhcasc:test :adminhcasc:assembleRelease --stacktrace
      - name: Sign APK (fallback)
        env:
          STORE_PASSWORD: ${{ secrets.STORE_PASSWORD }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
        run: |
          APK_IN=adminhcasc/build/outputs/apk/release/adminhcasc-release-unsigned.apk
          APK_OUT=adminhcasc/build/outputs/apk/release/adminhcasc-release.apk
          if [ -f "$APK_IN" ]; then
            /usr/local/lib/android/sdk/build-tools/34.0.0/apksigner sign \
              --ks /tmp/keystore.jks \
              --ks-pass pass:$STORE_PASSWORD \
              --key-pass pass:$KEY_PASSWORD \
              --ks-key-alias $KEY_ALIAS \
              --out "$APK_OUT" \
              "$APK_IN"
          else
            echo "WARNING: $APK_IN nenalezen"
          fi
          if [ ! -f "$APK_OUT" ] && [ -f "$APK_IN" ]; then
            cp "$APK_IN" "$APK_OUT"
          fi
      - name: Show artifacts
        run: ls -l adminhcasc/build/outputs/apk/release
      - name: Upload APK to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          password: ${{ secrets.DEPLOY_PASS }}
          source: adminhcasc/build/outputs/apk/release/adminhcasc-release.apk
          target: /tmp/adminhcasc-release.apk
      - name: Deploy APK
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
            password: ${{ secrets.DEPLOY_PASS }}
          script: |
            set -e
            if [ -d /tmp/adminhcasc-release.apk ]; then
              mv /tmp/adminhcasc-release.apk/adminhcasc-release.apk /tmp/adminhcasc-release.apk && rmdir /tmp/adminhcasc-release.apk || true
            fi
            test -f /tmp/adminhcasc-release.apk
            install -d /var/www/hotelapp/download /var/www/dagmar/download
            install -m 0644 /tmp/adminhcasc-release.apk /var/www/hotelapp/download/AdminApp.apk
            cp /var/www/hotelapp/download/AdminApp.apk /var/www/hotelapp/download/adminhcasc.apk
            cp /var/www/hotelapp/download/AdminApp.apk /var/www/hotelapp/download/admin.apk
            chown hotelapp:hotelapp /var/www/hotelapp/download/AdminApp.apk /var/www/hotelapp/download/adminhcasc.apk /var/www/hotelapp/download/admin.apk
            install -m 0644 /tmp/adminhcasc-release.apk /var/www/dagmar/download/AdminApp.apk
            cp /var/www/dagmar/download/AdminApp.apk /var/www/dagmar/download/adminhcasc.apk
            cp /var/www/dagmar/download/AdminApp.apk /var/www/dagmar/download/admin.apk
            chown www-data:www-data /var/www/dagmar/download/AdminApp.apk /var/www/dagmar/download/adminhcasc.apk /var/www/dagmar/download/admin.apk
            rm -f /tmp/adminhcasc-release.apk
